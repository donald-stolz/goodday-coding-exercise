import { PurchaseOrders } from '@prisma/client';

export const purchaseOrderCreatedTemplate = (purchaseOrder: PurchaseOrders) => {
  return `
`;
};

export const purchaseOrderUpdatedTemplate = (purchaseOrder: PurchaseOrders) => {
  return ``;
};
